# DEVLOG

Журнал разработки minecraft-quiz. Новые записи добавляются сверху.

## 2026-07-18 — v1.4.0: навигация на главный экран

По замечанию Айшота: после квиза нельзя было вернуться на главный экран —
только «Играть снова» сразу в новую игру.

- Финальный экран: кнопка «🏠 НА ГЛАВНУЮ» (без подтверждения — игра окончена).
- Экран результата вопроса: «🏠 НА ГЛАВНУЮ» с двухтаповым подтверждением
  («ТОЧНО ВЫЙТИ?») — игра ещё идёт, прогресс потеряется.
- Игровой экран: кнопка 🏠 в верхней панели, тоже с подтверждением («Выйти?»),
  взвод сбрасывается через 3 секунды — защита от случайного тапа.
- `goHome` сбрасывает префетч и инкрементирует `gameIdRef`, чтобы ответы
  запросов старой игры не всплывали после выхода; новая игра всегда с чистого
  листа (#1/10, ⭐ 0).
- E2E: выход через 🏠 из игры + рестарт (сценарий 1), выход с экрана
  результата (сценарий 2).

## 2026-07-18 — v1.3.1: бейдж версии в safe-area, судья прощает окончания

По скриншотам с iPhone 12 Pro:

- **Бейдж версии вылезал за край экрана**: `bottom/right` без учёта safe-area
  попадал в зону скругления дисплея и home-индикатора. Теперь бейдж внизу
  по центру, отступ `calc(8px + env(safe-area-inset-bottom))` с каскадным
  фолбэком (CSS-класс `.version-tag`), в мету добавлен `viewport-fit=cover`.
  E2E гоняется на вьюпорте iPhone 12 Pro (390×844) и проверяет, что бейдж
  вписан в экран и нет горизонтального overflow.
- **Ответ «Шёлковое касание» не был засчитан полностью**: ASR распознал
  «Шелков*ая* касание», судья снизил до 4/5 («в следующий раз точнее»).
  Причина: шкала «4 = по сути верно, но неточно» перевешивала общую просьбу
  прощать огрехи распознавания. Теперь и системный промпт судьи, и шкала
  явно требуют: расхождение только в окончаниях/роде/падеже/ё/регистре —
  полное совпадение, 5 баллов. Локальный fallback-скоринг аналогично:
  пословное совпадение по стемам без лишних слов = 5 (тест на кейс со скрина).

## 2026-07-18 — v1.3.0: номер версии на экране

- Введён счётчик версий: `APP_VERSION` в index.html + полупрозрачный бейдж
  `vX.Y.Z` в правом нижнем углу на всех экранах — удобно проверять на телефоне,
  что деплой Pages действительно доехал (а не кеш).
- Правило в CLAUDE.md: каждое изменение приложения в master поднимает версию
  (фичи — minor, фиксы — patch), версия упоминается в DEVLOG.
- Ретроспективная разметка: v1.0.0 — первая standalone-SPA на Pages (`2f76a55`),
  v1.1.0 — переход на DeepSeek + валидация ключа (`97b41f8`, `3dd0040`),
  v1.2.0 — релиз качества генерации, фиксов и тестов (`97a7c3b`).
- Тесты: юнит на формат версии + e2e-проверки бейджа на старте и в игре.

## 2026-07-18 — Качество AI-генерации, фиксы всех известных багов, тесты

Исправления (всё из списка «замечено» от 2026-07-17):

- **Найден и починен старый критический баг**: `\b` в JS-регулярках не работает
  с кириллицей — вся таблица ASR-коррекций (`MC_CORRECTIONS`) в проде никогда
  не срабатывала. Границы слов теперь строятся вручную (`[^а-яёa-z0-9]`),
  таблица вычищена (правила-пустышки и спорный «призрак»→«фантом» удалены,
  добавлен «ридстон»).
- Единый ввод ответа: голосовой транскрипт попадает в текстовое поле,
  отправляется ровно то, что видит игрок (раньше голос молча побеждал текст).
- Fallback-скоринг: числа и короткие слова больше не ломают оценку «4»
  («Минимум 10 блоков»), добавлен лёгкий стемминг для падежей, ё→е,
  числа сравниваются строго.
- Stale closures устранены: история вопросов и очередь тем в ref'ах,
  `gameIdRef` отсекает гонки старой/новой игры.
- Таймаут 15 с (AbortController) на все запросы; без ключа запросы к API
  вообще не делаются.
- `recognition.start()` обёрнут в try/catch (InvalidStateError).
- `callClaude` → `callAI`; мёртвый прототип `minecraft-quiz.jsx` удалён.

Улучшение генерации вопросов (провайдер не менялся — только промпты,
параметры запросов и логика выбора темы):

- Ротация 12 тем на стороне приложения (случайно, без повторов до исчерпания),
  тема передаётся в user-промпт.
- Новый системный промпт: Java Edition, только проверяемое по Minecraft Wiki,
  официальная русская локализация, самопроверка перед выводом,
  6 few-shot примеров; формат дополнен `acceptable_answers` (2–4 синонима).
- Температуры: 0.7 генерация, 0.2 оценка; `response_format: json_object`;
  `parseJSON` умеет вырезать JSON из окружающего текста.
- Судья получает `acceptable_answers` и засчитывает синонимы/разговорные
  формулировки; локальный скоринг тоже учитывает их (берёт максимум).
- Банк fallback-вопросов дополнен `acceptable_answers` для всех 15 вопросов.

Новые фичи (из идей прошлой сессии):

- Префетч следующего вопроса, пока игрок отвечает (латентность API скрыта;
  streaming не понадобился — префетч закрывает ту же проблему без изменения
  кода провайдера).
- Выбор сложности перед игрой (смешанная/легко/средне/сложно) — в промпт
  и в фильтр fallback-банка.
- Озвучка вопросов через SpeechSynthesis ru-RU: автоозвучка с переключателем
  (`mc_quiz_tts`) и кнопка «Прочитать вопрос».
- История результатов в `localStorage` (`mc_quiz_history`, последние 20),
  рекорд на стартовом экране и бейдж «Новый рекорд!» на финальном.

Тесты (`tests/`): 43 юнит-теста (Node VM + Babel, без рендера) и e2e
на playwright-core + системном Chromium (мок DeepSeek через route-перехват:
полный цикл с API и режим без ключа). Прогон перед пушем: `cd tests && npm test`.
Обновлён CLAUDE.md (архитектура, промпты, тесты, правила).

## 2026-07-17 — Документация проекта

- Аудит текущего состояния кода (index.html, история коммитов).
- Созданы `CLAUDE.md` (описание проекта, архитектура, правила работы)
  и `DEVLOG.md` (этот файл, история восстановлена по коммитам).
- Замечено на будущее (не исправлялось, фичи не добавлялись):
  - `handleSubmitAnswer` берёт `transcript || textInput` — если сначала ответил
    голосом, а потом напечатал текст, отправится голосовой вариант.
  - В fallback-оценке фильтр `w.length > 2` внутри `every()` ломает оценку «4»
    для ответов с короткими словами/числами («Минимум 10 блоков»: «10» не проходит
    проверку длины → `every` всегда false).
  - `generateQuestion` при «Играть снова» захватывает старый `askedQuestions`
    из замыкания (stale closure) — первый вопрос новой игры получает историю
    прошлой; повторный `start()` распознавания без паузы может кинуть
    InvalidStateError (не обёрнут в try/catch).
  - Нет таймаута на fetch — при зависшем API лоадер крутится вечно.
  - Часть правил `MC_CORRECTIONS` — no-op замены самих на себя («фантом» → «фантом»),
    а «призрак» → «фантом» может исказить легитимный ответ игрока.
  - `callClaude` — устаревшее имя (API давно DeepSeek), `minecraft-quiz.jsx` —
    мёртвый файл-прототип в корне деплоя.

## 2026-04-16 — Надёжность ключа и голосового ввода (`3dd0040`)

- Проверка API-ключа тестовым вызовом DeepSeek при сохранении, ошибка — на экран.
- `user-select: none` на кнопке микрофона — текст не выделяется при удержании.
- Таблица `MC_CORRECTIONS`: фонетические исправления Minecraft-терминов
  в русском транскрипте ASR («ридстон» → «редстоун», «пиглен» → «пиглин» и т.д.).

## 2026-04-16 — Переход с Anthropic API на DeepSeek (`97b41f8`)

- Endpoint → `https://api.deepseek.com/chat/completions`, модель → `deepseek-chat`.
- Заголовки и формат: `Authorization: Bearer`, system-сообщение в `messages`,
  ответ читается из `choices[0].message.content` (OpenAI-совместимо).
- Снята валидация префикса ключа `sk-ant-`.

## 2026-04-15 — Standalone-версия для GitHub Pages (`2f76a55`)

- `index.html`: SPA без сборки — React 18 UMD + Babel standalone с CDN,
  весь код прототипа перенесён внутрь одного файла.
- Добавлен ввод API-ключа на стартовом экране с хранением в `localStorage`
  (в прототипе-артефакте ключ не требовался).

## 2026-04-15 — Начало проекта (`b903a9b`)

- Первый коммит: `minecraft-quiz.jsx` — прототип викторины из Claude-артефакта
  (React-компонент: 10 вопросов, голосовой ввод ru-RU, AI-генерация и AI-оценка,
  fallback-банк вопросов) + локальные настройки Claude Code.
